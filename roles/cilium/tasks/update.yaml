---
- name: Import facts
  ansible.builtin.include_role:
    name: '{{ item }}'
    tasks_from: facts
  loop:
    - cloudflare
    - k3s

- name: Chart Update
  when: ansible_host == k3s_server_default_host
  block:
    - name: Create hubble ui ingress
      kubernetes.core.k8s:
        definition:
          apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            annotations:
              cert-manager.io/cluster-issuer: '{{ cloudflare_cluster_issuer }}'
            name: '{{ cilium_vars.kubernetes.hubble.ui.ingress.service }}'
            namespace: '{{ cilium_vars.kubernetes.namespace }}'
          spec:
            ingressClassName: cilium
            rules:
              - host: '{{ cilium_vars.kubernetes.hubble.ui.ingress.subdomain }}.{{ cloudflare_vars.kubernetes.gateway.domain }}'
                http:
                  paths:
                    - backend:
                        service:
                          name: '{{ cilium_vars.kubernetes.hubble.ui.ingress.service }}'
                          port:
                            name: http
                      path: /
                      pathType: Prefix
            tls:
              - hosts:
                  - '{{ cilium_vars.kubernetes.hubble.ui.ingress.subdomain }}.{{ cloudflare_vars.kubernetes.gateway.domain }}'
                secretName: '{{ cloudflare_vars.kubernetes.gateway.cluster.issuer.key.prefix }}-{{ cilium_vars.kubernetes.hubble.ui.ingress.service }}'
        kubeconfig: '{{ k3s_vars.directory.config }}/k3s.yaml'
        wait: true
      when: cloudflare_vars.kubernetes.gateway.domain != 'disabled'

    - name: Set hubble ui loadbalancer ip
      kubernetes.core.k8s:
        definition:
          apiVersion: v1
          kind: Service
          metadata:
            annotations:
              io.cilium/lb-ipam-ips: '{{ cilium_vars.kubernetes.hubble.ui.loadbalancer.ip }}'
            name: cilium-ingress-{{ cilium_vars.kubernetes.hubble.ui.ingress.service }}
            namespace: '{{ cilium_vars.kubernetes.namespace }}'
        kubeconfig: '{{ k3s_vars.directory.config }}/k3s.yaml'
        wait: true
      when: cloudflare_vars.kubernetes.gateway.domain != 'disabled'

    - name: Set hubble ui loadbalancer ip
      kubernetes.core.k8s:
        definition:
          apiVersion: v1
          kind: Service
          metadata:
            annotations:
              io.cilium/lb-ipam-ips: '{{ cilium_vars.kubernetes.hubble.ui.loadbalancer.ip }}'
            name: '{{ cilium_vars.kubernetes.hubble.ui.ingress.service }}'
            namespace: '{{ cilium_vars.kubernetes.namespace }}'
          spec:
            type: LoadBalancer
        kubeconfig: '{{ k3s_vars.directory.config }}/k3s.yaml'
      when: cloudflare_vars.kubernetes.gateway.domain == 'disabled'

    - name: Set hubble tls
      kubernetes.core.helm:
        chart_ref: '{{ cilium_vars.kubernetes.helm.repository.name }}/{{ cilium_vars.kubernetes.helm.chart.name }}'
        chart_version: '{{ cilium_vars.kubernetes.helm.chart.version }}'
        kubeconfig: '{{ k3s_vars.directory.config }}/k3s.yaml'
        name: '{{ cilium_vars.kubernetes.helm.chart.name }}'
        namespace: '{{ cilium_vars.kubernetes.namespace }}'
        reset_values: false
        reuse_values: true
        values:
          hubble:
            tls:
              auto:
                certManagerIssuerRef:
                  group: cert-manager.io
                  kind: ClusterIssuer
                  name: '{{ cloudflare_cluster_issuer }}'
                method: '{{ cilium_vars.kubernetes.hubble.tls.method }}'
        wait: true
      register: result
      delay: 1
      retries: 3
      until: result is not failed
      when:
        - cilium_vars.kubernetes.hubble.tls.method == 'certmanager'
        - cloudflare_vars.kubernetes.gateway.domain != 'disabled'

    - name: Enable metrics
      kubernetes.core.helm:
        chart_ref: '{{ cilium_vars.kubernetes.helm.repository.name }}/{{ cilium_vars.kubernetes.helm.chart.name }}'
        chart_version: '{{ cilium_vars.kubernetes.helm.chart.version }}'
        kubeconfig: '{{ k3s_vars.directory.config }}/k3s.yaml'
        name: '{{ cilium_vars.kubernetes.helm.chart.name }}'
        namespace: '{{ cilium_vars.kubernetes.namespace }}'
        reset_values: false
        reuse_values: true
        values:
          dashboards:
            enabled: true
          envoy:
            prometheus:
              serviceMonitor:
                enabled: true
          hubble:
            metrics:
              dashboards:
                enabled: true
              enabled:
                - dns:query;ignoreAAAA
                - drop
                - flow
                - httpV2
                - icmp
                - port-distribution
                - tcp
              serviceMonitor:
                enabled: true
            relay:
              prometheus:
                serviceMonitor:
                  enabled: true
          operator:
            prometheus:
              enabled: true
              serviceMonitor:
                enabled: true
          prometheus:
            enabled: true
            serviceMonitor:
              enabled: true
        wait: true
      register: result
      delay: 1
      retries: 3
      until: result is not failed
