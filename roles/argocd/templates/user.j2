apiVersion: v1
kind: ConfigMap
data:
  accounts.{{ argocd_vars.kubernetes.server.user.name }}: apiKey, login
  accounts.{{ argocd_vars.kubernetes.server.user.name }}.enabled: 'true'
metadata:
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
  name: argocd-cm
  namespace: {{ argocd_vars.kubernetes.namespace }}
