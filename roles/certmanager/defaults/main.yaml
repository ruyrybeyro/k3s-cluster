---
certmanager_vars:
  kubernetes:
    helm:
      chart:
        name: cert-manager
        version: v1.14.3
      repository:
        name: jetstack
        url: https://charts.jetstack.io
    namespace: kube-system
